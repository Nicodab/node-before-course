const num1 = 5
const num2 = 10

function addValues() {
    console.log('the sum is:  ${num1 + num2}')
}

// Just call it here if it doesn't return anything
// --> a const or variable dont need to be here for the require
// ---> we just call require by itself
addValues()